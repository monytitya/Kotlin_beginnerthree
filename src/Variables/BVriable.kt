package Variables

//val → Immutable (read-only)
//var → Mutable (can change)

fun main(){
    var age = 25 //can to change
    val  test = 20 //can't to change

    age = 26  // ត្រឹមត្រូវ
    age = 27  // ត្រឹមត្រូវ
    println(age)
    print(test)
}
